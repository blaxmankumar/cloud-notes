# Unauthenticated user

Use a clean browser profile with no Identity Center cookies and open the application.

Expected: redirect to IAM Identity Center or deny. The application must not receive the original request before successful authentication and policy evaluation.
