apiVersion: v1
kind: Service
metadata:
  name: cloud-notes-frontend-service
  namespace: cloud-notes
spec:
  selector:
    app: cloud-notes-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
